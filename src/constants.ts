import { EmbeddingModel } from "openai/resources/embeddings";
import { ChatModel } from "openai/resources/shared";

export const GPT_MODEL: ChatModel = "gpt-5-nano";

export const EMBEDDING_MODEL: EmbeddingModel = "text-embedding-3-small";
